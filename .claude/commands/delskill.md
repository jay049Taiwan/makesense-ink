---
description: 刪除指定的 skill。 觸發詞：`/delskill`、「刪掉這個 skill」「移除 skill」。 用法：/delskill "<名稱>" disable-model-invocation: true argument-hint: "<skill名稱>"
---

請依使用者輸入的 $ARGUMENTS 觸發並執行 skill `delskill`。優先使用 Skill tool 載入 `delskill` 並按其 SKILL.md 規範執行。
