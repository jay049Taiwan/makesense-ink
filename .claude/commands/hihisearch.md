---
description: 嗨嗨搜查 — 嗨嗨家族成員、makesense 的採集員/補給站。職責是讓對的資料以對的格式進到知識庫（DB06/DB07/DB08），不負責分析。 核心能力 2 種：專題研究（別人的 URL→抓網頁內容+合法檔案）/ 數據紀錄（自家 URL→抓數字指標）。 特殊建檔權限：可新建 DB08 page、可新建 DB01 提案頁（須四九同意）。建檔前必查重對應連結是否已存在 DB06。 觸發詞：`/h
---

請依使用者輸入的 $ARGUMENTS 觸發並執行 skill `hihisearch`。優先使用 Skill tool 載入 `hihisearch` 並按其 SKILL.md 規範執行。
