---
description: DB 級全套稽核入口。集成 nsc + nvc + npcc 三合一(+ check3-5 選配),對指定 DB 跑欄位設計 / 資料填寫 / page 內文 / 跨系統同步全套健檢。 觸發詞：`dbcheck`、「DB 健檢」「整 DB 稽核」「dbcheck DB05」「dbcheck 快」「dbcheck 同步」「dbcheck DB05 全套」。 與 hihicheck 區分:hihich
---

請依使用者輸入的 $ARGUMENTS 觸發並執行 skill `dbcheck`。優先使用 Skill tool 載入 `dbcheck` 並按其 SKILL.md 規範執行。
