---
description: DB 級「未抓資料」掃描器。掃 Notion DB 找出有 source URL 但內容欄位空、或應有 source URL 但漏抓的 page,列待辦清單,引導四九用 hihisearch 逐筆抓。 觸發詞：`dbsearch`、「找未抓的引用」「掃 DB URL 待辦」「dbsearch DB06」「dbsearch DB06 自動」。 與 hihisearch 區分:hihisearch 接
---

請依使用者輸入的 $ARGUMENTS 觸發並執行 skill `dbsearch`。優先使用 Skill tool 載入 `dbsearch` 並按其 SKILL.md 規範執行。
