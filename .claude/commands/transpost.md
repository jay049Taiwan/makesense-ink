---
description: 標記「後面接的文字是我從別的 session 轉貼過來的內容」，把它當成跨 session 帶進來的素材接收。 觸發詞：`/transpost`、「這是別的 session 轉來的」「轉貼過來」。 用法：/transpost <從其他 session 複製貼上的內容> disable-model-invocation: true argument-hint: <從其他 session 轉貼的內容>
---

請依使用者輸入的 $ARGUMENTS 觸發並執行 skill `transpost`。優先使用 Skill tool 載入 `transpost` 並按其 SKILL.md 規範執行。
