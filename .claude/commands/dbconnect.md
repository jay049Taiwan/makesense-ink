---
description: DB 級「未補 X引用」掃描器。掃 Notion DB 找出 9 個 X引用 欄位全空的 page,列待辦清單,引導四九用 hihiconnect 逐筆補。 觸發詞：`dbconnect`、「找未補 X引用 的 page」「dbconnect DB05」「dbconnect DB05 partial」「dbconnect DB05 自動」。 與 hihiconnect 區分:hihiconnect
---

請依使用者輸入的 $ARGUMENTS 觸發並執行 skill `dbconnect`。優先使用 Skill tool 載入 `dbconnect` 並按其 SKILL.md 規範執行。
