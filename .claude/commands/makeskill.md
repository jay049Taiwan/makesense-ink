---
description: 把一段描述（或本對話剛剛的流程）做成一個新的 skill，同時就是 slash command。 觸發詞：`/makeskill`、「做成 skill」「把這個變成 skill」「存成 skill」。 用法：/makeskill "<名稱>" <這個 skill 以後要做什麼> 例：/makeskill "abcde" 以後都把接到的內容重點再加 50 字說明 disable-model-invo
---

請依使用者輸入的 $ARGUMENTS 觸發並執行 skill `makeskill`。優先使用 Skill tool 載入 `makeskill` 並按其 SKILL.md 規範執行。
