---
type: project
date: 2026-05-05
scope: Notion 子頁面深度稽核 C 組（DB01/02/03/04/09 + DB欄位字典 + 報表 3 份）
originSessionId: a7d43a70-af42-47c9-9677-a6a2e1de0f31
---

> ⚠️ **2026/05/09 標註**：此為歷史稽核報告（2026-04-28 ~ 2026-05-05 期間產出）。所述「現行」schema 已於 2026/05/06~05/08 大幅改名（互動選項→互動類別、登記選項→登記類別、文案細項→文案選項、內容選項→素材類別、預約報名→填寫報名、請款請購→紀錄費用、活動細項→活動選項、走讀行旅→導覽走讀、紀錄備項→紀錄細項；DB05 大分類「圖文影音」→「內容素材」）。**僅供歷史脈絡，不要當作現行 schema 依據；現況以 MEMORY.md / makesense-ink/CLAUDE.md 為準**。

---

# Notion 子頁面 vs DB 實 schema 稽核（C 組）

## 對照基準
- DB01 collection: `722f2478-7e61-4b4b-ad1c-d171b4a639db`（title=`專案名稱`）
- DB02 collection: `c286e19b-9cf8-422b-8628-98b6d116040c`（title=`管考名稱`）
- DB03 collection: `968b23ea-da1f-4381-bd9a-253ee80b0656`（title=`項目名稱`）
- DB04 collection: `5ad63416-a7c5-4d84-812e-cddf56c8bc01`（title=`協作名稱`，共 219 屬性）
- DB09 collection: `6547375e-ff14-4f24-ab0f-9f2a223a8580`（title=`紀錄名稱`）

---

## 1. DB01 維護指南 `3179ff25fdab800ab809e7eaa40d21f1`

🔴 **整頁空白（blank-block）**。Notion 上完全沒有內容。實 schema 顯示 DB01 共 50+ 屬性，包括重要欄位 `專案名稱`(title)、`提案類型`（承攬申請/委託參與/自提自辦）、`參與屬性`（學校委託/民間委託/小額採購/公開招標/獎補助/委託專業服務/委託服務採購/勞務採購/後續擴充）、`執行狀態`（預計提案/提案送件/簡報複試/完成提案未送件/無提案/完成撤案/提案通過/未通過）、`標案案號`、`對應委託對象`、`對應提案品牌`、`對應提案標籤`、`需求定位`、`條件評估`、`簡介摘要`、`目標營收`、`營業額`、`提案費用`(rollup)、`預計毛利/最後毛利/最後毛率`(formula)、`提案倒數`(formula)、`引用文獻`(formula)、`資料檢核`(formula)、`對應交接/管考/項目/明細/表單/參考資料`、9 個 ai 狀態欄位等。

**致命問題（1 條）**：整頁無內容，與其他 8 份 DB 指南不對等。

---

## 2. DB02 維護指南 `2799ff25fdab80858d05cabb2cebefd3`

🔴 致命：
- 指南欄位名 `對應類型`，但實 schema 是 `管考類型`（select：對應民間/對應公部門/對應自提）。CLAUDE.md「已知修正」說 DB02 是「對應類型」實為「管考類型」——指南本身仍寫錯。
- 指南列「**對應交接** → DB04 共識交接協作」OK；但提到「**對應封存** → 結案封存資料」、「**累計執行費用** → 費用流水帳」——這兩個欄位（`對應封存`、`累計執行費用`）實 schema 中：`對應封存` 存在（連 collection `fe14f230-...`），但「累計執行費用」名稱不存在，實際是 `提案管考費用`/`項目預算總計`/`預算總計`/`雜支實報` 等多個 rollup。

🟡 過時：
- 指南 4 處仍寫舊名「DB04 共識交接協作」「DB03 工作項目進度」——CLAUDE.md 已校正為「協作交接」「項目進度」（Notion 實際標題為 `DB04交接協作`、`DB03項目進度`）。
- 指南未列實 schema 重要欄位：`簡介摘要`、`執行目標`、`項目進度/預計進度/進度計算`(formula)、`損益判斷`、`費用總計/雜支預算`、`對應標籤`、`對應表單`、`對應明細`、`對應委託單位`(rollup)、`相關出貨/雜支實報`(rollup)、9 ai 欄位、ai_meta/ai_對應對象/ai_對應標籤。
- 指南「日期狀態：暫定/無限期/確定」欄位 — 實 schema 不存在此欄位（已被移除/改名）。
- 「責任執行 Person」OK 但指南另一段重複列。

🟢 次要：兩段「主要欄位一覽」與「關鍵欄位說明」內容重疊但描述不一致。

---

## 3. DB03 維護指南 `2799ff25fdab803a9682d56a2acbb271`

🔴 致命：
- 指南「下層關聯：→ DB04（透過對應項目）」描述錯誤。實 schema 中 DB03 連 DB04 的 relation 名稱是 **`對應協作`**（不是「對應項目」）。「對應項目」這名字反而被 DB02、DB04、DB05、DB06 等用來連 DB03。指南把方向搞混了。
- 指南列 DB03「主要欄位一覽：對應項目 Relation → DB04」— 同上，實際是 `對應協作`。
- 指南最後一段 n8n 邏輯仍提「DB04 自身的『登記發佈』」— 實 schema DB04 的單場層欄位不叫「登記發佈」（實際 DB04 沒有「登記發佈」屬性；DB05/DB06 才有）；DB04 的對應發佈控制是 `下架選項` + `對應發佈品牌`。「登記發佈」屬於 DB05/DB06 共用欄位。

🟡 過時：
- 「執行狀態：執行中/已預定/已完成」OK 與 schema 一致；但指南舊版區塊提「離開執行中→全部下架」描述 WP 行為，現已遷 makesense.ink Next.js（CLAUDE.md 註明）。
- 指南未列重要欄位：`簡介摘要`、`項目類型`(自辦/委外)、`參與費用`(需收費/無費用/已收費)、`線上同步`(需線上/無線上/已上線)、`量化指標/質化指標`、`對應提案/標籤/封存/表單/明細`、`完成度/實際進度/預計進度/啟動日期/執行倒數/執行天數/帶動零售`(formula 群)、9 ai 欄位、`外包合作`。
- 指南文末舊連結「WP官網同步架構指南」+ 舊 ID `2799ff25fdab80888019f03cd55fd875` 連到 DB04 而非「官網維護指南」`32c9ff25fdab81389368eac6f77bc417`——應更新。

🟢 兩處重複「核心控制欄位」區塊。

---

## 4. DB04 維護指南 `2799ff25fdab80888019f03cd55fd875`

🔴 致命：
- 指南「Collection ID：`99808f9c-d0ab-4c21-bc68-4d8836150207`」— 這是 page_id 不是 collection_id。實際 DB04 collection ID 是 **`5ad63416-a7c5-4d84-812e-cddf56c8bc01`**。AI Agent 若按指南寫入會找不到 DB。
- 指南標題欄位寫 `交接名稱`，實 schema title 是 **`協作名稱`**（CLAUDE.md 已知修正）。指南表格內仍兩處用「交接名稱」。
- 指南「協作選項只有兩個合法值：活動辦理 / 內容製作」— 實 schema 的 `協作選項` 是 multi_select 欄位，且決策樹（`3569ff25fdab81a4aa32e9a851f3af7b`）寫的合法值與此不符（決策樹用 `互動選項`=內部共識/聯絡互動/行政事務 來分流，不是「協作選項」二分）。指南與決策樹不一致。
- 指南「庫存門市操作規範：庫存選項=進貨→進貨細項；=出貨→出貨細項；=盤點→盤點細項」— 實 schema 三個都有，OK。但指南未提另兩個必填路徑：`門市選項`（盤點檢查/場地使用/保養維護/值班顧店）。

🟡 過時/缺漏：
- 指南未提及實 schema 的這些重要欄位（CLAUDE.md「已知修正」應該已加入）：`協作名稱`(title)、`活動細項`、`實際單價/預計單價`、`實際數量/預計數量`、`登記發佈`、`距離km`、`最低數量`、`簡介摘要`、`對應辦理單位`、`執行時間`(date 含 end → duration_min 邏輯)。指南完全沒寫這些。
- 指南列 `費用收退`、`費用撥用`、`請款請購` 選項 OK 但實 schema `請款請購` 在「DB欄位字典」標為 DB04/05/06 共用，指南未說明跨 DB 一致性。
- 指南「逾期判斷邏輯」用 `執行時間` 已過今日 — OK。

🟢 「執行備註 AI 配對寫入格式」是純文字 template，無 schema 衝突。

---

## 5. DB09 維護指南 `3179ff25fdab804cad84dc6153aa8c67`

🟡 此頁未在前序批次抓到完整內容（fetch 回傳的是同 ID 但內容不可見/blank-page 之外的判讀僅靠決策樹）。實 schema 為 9 DB 中最龐大的「日期骨幹」DB（80+ 屬性）：title=`紀錄名稱`、`紀錄類型`(Day/Week/Month/Season/Year/歷史事件)、`範圍日期`、`時間鍵`、`上層項目/下層項目/週` 自關聯、對應 7 個 DB 各 4 種日期錨點（建立/起算/執行/截止）共 28 個 relation、各種品牌/合作/批售/書籍/文宣的進貨/出貨/採購/營收/宣傳 rollup 群、簡介摘要、9 ai 欄位。

🔴 若指南內容極簡（決策樹頁面 `3569ff25fdab81b880ecf9de59b72739` 標題即「DB09 日期骨幹說明（單純，無需決策樹）」），實 schema 的複雜度與「無需決策樹」聲明不符——應補完整欄位字典。

---

## 6. DB欄位字典 `32b9ff25fdab81caadd5e016cced1efe`

整體很完整（涵蓋 9 DB 跨表共用 + 各 DB 獨有 + 「疑似可清除」清單）。

🔴 致命：
- DB04 獨有清單列 `交接名稱`、`交接類型`、`交接格式`、`交接回覆` — 但 title 已改為 `協作名稱`（CLAUDE.md 已修正）。字典裡的 `交接名稱` 應在「DB04 獨有」中刪除，改列 `協作名稱`；或同時保留並註記過渡。
- DB04 獨有清單缺 `協作名稱`（實 schema 確有此欄位，作為新 title）。
- DB02 獨有列「對應類型」（CLAUDE.md 已知 DB02 真實欄位是 `管考類型`）— 應為 `管考類型`。

🟡 過時：
- 「共同欄位」表把 `對應交接` 列為 DB01, DB02（2 個）— 實際 DB05、DB06 也有 relation 連 DB04，但欄位名可能不同（DB05 用 `對應協作`、DB06 用 `對應協作`）。命名不一致導致字典低估覆蓋率。
- 「疑似可清除」清單的值班顧店欄位（`烊1/烊10/開1/打烊500元` 等 30+ 個）— 確實該清除，但這份報告本身的存在說明這些欄位仍未被刪除（schema 仍在）。提醒四九執行清理。

🟢 「3-1 分層閱讀順序」「3-2 七類聯想訊號」描述完整，無 schema 衝突。

---

## 7. 日報表指南 `3229ff25fdab8045a98ed2b8ce920909`

未抓到內容（fetch 在批次中疑因索引頁緩存替換）。報表規格指南（下方）已完整覆蓋日報表 L2 規格。

🟢 需獨立檢查；若 `3229ff25fdab8045a98ed2b8ce920909` 內容實質與 `3239ff25fdab81418330e0593db065f4` 重複，建議合併或在日報表頁加 redirect 註記。

---

## 8. 報表規格指南 `3239ff25fdab81418330e0593db065f4`

🟡 過時：
- 「Notion Agent 使用 Claude 最新版本模型（目前為 claude-opus-4-6）」— 現行（2026/05）已是 Claude Opus 4.7（系統環境已標 `claude-opus-4-7[1m]`）。需更新版本標示。
- 多處仍用舊 DB 名稱：「**登記表單明細**」「**庫存資產控管**」「**專案執行管考**」（2026/04 已校正為登記表單/庫存控管/績效管考）。
- L1/L2/L3 範例用「銷售」「進貨」標籤 OK，但「來源：登記表單明細 Agent」應改「DB05 登記表單 Agent」。
- 「**收費狀況=需收費**」OK，與 DB05/06 schema 一致。「**收支認列=收入**」OK。
- 「修訂紀錄 2026/03/14」距今近 2 個月，期間多次 schema 變動未反映。

🟢 整體格式範本（L1-L5）結構良好。

---

## 9. LINE 官方帳號指南 `34c9ff25fdab80bfabebf533702f0dfc`

🔴 **幾乎空白**：頁內僅 4 字「六大功能」+ empty-block。LINE 整合是 makesense.ink 主力（CLAUDE.md `line_integration_handoff.md` 已交接、Member session 已上線），此指南完全未填內容。

---

## 最嚴重 3 條（優先修）

1. **DB04 指南把 page_id 寫成 Collection ID**（`99808f9c...` 是 page_id；正確 collection 是 `5ad63416-...-cddf56c8bc01`）。AI Agent 按此寫入會找不到 DB。位於 `2799ff25fdab80888019f03cd55fd875` 「快速對照表」與「庫存門市操作規範」上方。

2. **DB03 指南把連 DB04 的 relation 名寫反**（指南寫「對應項目→DB04」，實際是「`對應協作`→DB04」）。位於 `2799ff25fdab803a9682d56a2acbb271` 「主要欄位一覽」、「層級關係」、「與其他 DB 的關聯說明」三處。

3. **DB01 維護指南整頁空白 + LINE 官方帳號指南幾乎空白**。兩份正在運作中的核心系統（DB01 提案來源、LINE 是會員/通知主通道）完全沒有指南文件。位於 `3179ff25fdab800ab809e7eaa40d21f1`、`34c9ff25fdab80bfabebf533702f0dfc`。

---

## 各份條數彙總

| 子頁 | 🔴 | 🟡 | 🟢 |
|---|---|---|---|
| DB01 指南 | 1（整頁空白） | - | - |
| DB02 指南 | 2 | 4 | 1 |
| DB03 指南 | 3 | 3 | 1 |
| DB04 指南 | 4 | 3 | 1 |
| DB09 指南 | 1（疑似簡略） | - | - |
| DB欄位字典 | 3 | 2 | 1 |
| 日報表指南 | - | 1（疑與報表規格重疊） | - |
| 報表規格指南 | - | 4 | 1 |
| LINE 指南 | 1（幾乎空白） | - | - |
