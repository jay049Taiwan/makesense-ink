import type { Metadata } from "next";
export const metadata: Metadata = { title: "空間體驗 | 旅人書店", description: "旅人書店空間租借與體驗服務，適合講座、展覽、工作坊。" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
